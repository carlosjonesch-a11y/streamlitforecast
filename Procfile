## Procfile disabled — Heroku deployment is no longer used. The app is deployed to Streamlit Cloud.
## If you still want to use Heroku, restore this file or create a new Procfile with the following content:
## web: sh -c "streamlit run app.py --server.port $PORT --server.address 0.0.0.0"