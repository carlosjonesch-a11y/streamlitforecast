streamlit
pandas
numpy
plotly
scikit-learn
openpyxl
statsmodels

# Minimal default reqs for Streamlit Cloud  excludes Prophet and other heavy
# compiles. The app supports Prophet optionally and will show a friendly
# warning if Prophet is not available. If you need Prophet, install it locally
# or add it to this file and re-deploy.
